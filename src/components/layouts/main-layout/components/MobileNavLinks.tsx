import { Button } from "@/components/ui/common/shadcn/button";

function MobileNavLinks() {
  return (
    <Button
      className="flex items-center px-3 font-bold hover:bg-stone-500"
      onClick={() => null}
    >
      Logout
    </Button>
  );
}

export default MobileNavLinks;
